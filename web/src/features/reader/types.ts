export type ReaderSearch = {
  albumId: string
  page?: number
  sourceId?: string
  mangaKey?: string
}

export type ReaderChapterItem = {
  id: string
  title: string
}

export type ReaderWindowPage = {
  index: number
  src: string
}
